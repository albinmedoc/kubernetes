
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: portainer
  namespace: portainer-web
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`portainer.albinmedoc.se`)
      kind: Rule
      services:
        - name: portainer
          port: 9000