apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: photoprism
  namespace: photoprism-minnen
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: truenas-nfs
  resources:
    requests:
      storage: 20Gi