apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: immich
  namespace: immich-resor
  labels:
    app.kubernetes.io/name: immich
    app.kubernetes.io/instance: resor
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`resor.albinmedoc.se`)
      kind: Rule
      services:
        - name: immich
          port: 2283