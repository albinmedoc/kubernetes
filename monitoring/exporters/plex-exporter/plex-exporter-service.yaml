apiVersion: v1
kind: Service
metadata:
  name: plex-exporter
  namespace: monitoring
  labels:
    app.kubernetes.io/name: plex-exporter
spec:
  type: ClusterIP
  ports:
  - port: 9594
    targetPort: metrics
    protocol: TCP
    name: metrics
  selector:
    app.kubernetes.io/name: plex-exporter
