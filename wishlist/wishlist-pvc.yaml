apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: wishlist
  namespace: wishlist
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: truenas-nfs
  resources:
    requests:
      storage: 5Gi