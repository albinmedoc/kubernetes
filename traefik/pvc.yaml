apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: traefik-storage-pvc
  namespace: traefik
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
  storageClassName: traefik