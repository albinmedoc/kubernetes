apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: isponsorblocktv
  namespace: isponsorblocktv
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: truenas-nfs
  resources:
    requests:
      storage: 512Mi