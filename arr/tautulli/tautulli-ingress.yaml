apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: tautulli
  namespace: arr
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`tautulli.flexflix.top`)
      kind: Rule
      services:
        - name: tautulli
          port: 8181