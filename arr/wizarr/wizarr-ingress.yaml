apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: wizarr
  namespace: arr
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`join.flexflix.top`)
      kind: Rule
      services:
        - name: wizarr
          port: 5690