
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: gatus
  namespace: gatus
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`status.albinmedoc.se`)
      kind: Rule
      services:
        - name: gatus
          port: 8080